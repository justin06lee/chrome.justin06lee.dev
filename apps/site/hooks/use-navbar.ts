// Bridges the @/hooks/use-navbar alias (used by registry component source) to the
// canonical hook in packages/registry, mirroring lib/utils.ts.
export { useNavbar } from "../../../packages/registry/navbar/use-navbar";
export type { UseNavbarReturn } from "../../../packages/registry/navbar/use-navbar";
