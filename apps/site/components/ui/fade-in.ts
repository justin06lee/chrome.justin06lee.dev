// Bridges the @/components/ui/fade-in alias (used by registry source that composes
// the fade, e.g. article-list's card stagger) to the canonical component.
export * from "../../../../packages/registry/fade-in/fade-in";
