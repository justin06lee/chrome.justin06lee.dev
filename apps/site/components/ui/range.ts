// Bridges the @/components/ui/range alias (used by registry source that composes
// the range slider, e.g. image-cropper) to the canonical component.
export * from "../../../../packages/registry/range/range";
