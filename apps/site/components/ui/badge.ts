// Bridges the @/components/ui/badge alias (used by registry source that composes
// the badge, e.g. gallery / article-list / tag-input) to the canonical component.
export * from "../../../../packages/registry/badge/badge";
