// Bridges the @/components/ui/file-card alias (used by registry source that
// composes the card, e.g. file-grid) to the canonical component in packages/registry.
export * from "../../../../packages/registry/file-card/file-card";
