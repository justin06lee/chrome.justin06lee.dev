// Bridges the @/components/ui/segmented alias (used by registry source that
// composes the segmented control, e.g. calendar-nav) to the canonical component.
export * from "../../../../packages/registry/segmented/segmented";
