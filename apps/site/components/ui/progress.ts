// Bridges the @/components/ui/progress alias (used by registry source that
// composes the progress bar, e.g. dropzone) to the canonical component.
export * from "../../../../packages/registry/progress/progress";
