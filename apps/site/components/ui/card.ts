// Bridges the @/components/ui/card alias (used by registry source that composes
// the card, e.g. gallery) to the canonical component in packages/registry.
export * from "../../../../packages/registry/card/card";
