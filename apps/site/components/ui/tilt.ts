// Bridges the @/components/ui/tilt alias (used by registry source that composes
// the tilt, e.g. pfp) to the canonical component in packages/registry.
export * from "../../../../packages/registry/tilt/tilt";
