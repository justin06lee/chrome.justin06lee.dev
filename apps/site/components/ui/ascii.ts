// Bridges the @/components/ui/ascii alias (used by registry source that
// composes it, e.g. not-found) to the canonical component in packages/registry.
export { Ascii } from "../../../../packages/registry/ascii/ascii";
export type { AsciiProps } from "../../../../packages/registry/ascii/ascii";
