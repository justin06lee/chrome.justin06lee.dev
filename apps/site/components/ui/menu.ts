// Bridges the @/components/ui/menu alias (used by registry source that composes
// the menu, e.g. gallery) to the canonical component in packages/registry.
export * from "../../../../packages/registry/menu/menu";
