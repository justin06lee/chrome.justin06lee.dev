// Bridges the @/components/ui/stack alias (used by registry source that
// composes the stack, e.g. file-card) to the canonical component in packages/registry.
export * from "../../../../packages/registry/stack/stack";
