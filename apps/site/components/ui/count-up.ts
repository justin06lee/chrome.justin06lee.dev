// Bridges the @/components/ui/count-up alias (used by registry source that
// composes the counter, e.g. stat-tile) to the canonical component in packages/registry.
export * from "../../../../packages/registry/count-up/count-up";
