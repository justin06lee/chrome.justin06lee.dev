// Bridges the @/components/ui/dialog alias (used by registry source that composes
// the promise-based dialog, e.g. manager-table) to the canonical component.
export * from "../../../../packages/registry/dialog/dialog";
