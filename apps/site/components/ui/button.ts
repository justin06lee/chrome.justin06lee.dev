// Bridges the @/components/ui/button alias (used by registry source that composes
// the button, e.g. calendar-nav) to the canonical component in packages/registry.
export * from "../../../../packages/registry/button/button";
